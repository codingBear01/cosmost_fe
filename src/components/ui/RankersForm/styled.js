/* libraries */
import styled from 'styled-components';
/* static data */
import {
  COLOR_LIST as color,
  FONT_SIZE_LIST as fs,
  BORDER_RADIUS_LIST as br,
  GAP_LIST as gap,
} from '../../../style';

export const RankersType = styled.span`
  align-self: start;
  font-size: ${fs.xl};
  font-weight: 600;
`;
