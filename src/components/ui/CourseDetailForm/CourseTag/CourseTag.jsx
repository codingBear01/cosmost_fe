/* libraries */
import React from 'react';
/* components */
import * as S from './styled';

function CourseTag({ children }) {
  return <S.StyledCourseTag>{children}</S.StyledCourseTag>;
}

export default CourseTag;
