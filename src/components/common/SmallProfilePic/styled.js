import styled from 'styled-components';
import { BORDER_RADIUS_LIST as br } from '../../../style';

export const StyledSmallProfilePic = styled.img`
  width: 3.5rem;
  height: 3.5rem;
  border-radius: ${br.circle};
`;
