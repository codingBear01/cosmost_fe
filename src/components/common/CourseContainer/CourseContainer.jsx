import React from 'react';
import * as S from './styled';

function CourseContainer({ children }) {
  return <S.StyledCourseContainer>{children}</S.StyledCourseContainer>;
}

export default CourseContainer;
