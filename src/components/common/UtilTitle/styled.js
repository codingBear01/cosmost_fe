import styled from 'styled-components';
import {
  COLOR_LIST as color,
  BORDER_RADIUS_LIST as br,
  BUTTON_SIZE_LIST as bs,
  FONT_SIZE_LIST as fs,
  GAP_LIST as gap,
  BREAK_POINTS as media,
} from '../../../style';

export const StyledUtilTitle = styled.span`
  align-self: start;
  margin-bottom: 50px;
  font-size: 20px;
  font-weight: 600;
  color: ${color.white};
`;
