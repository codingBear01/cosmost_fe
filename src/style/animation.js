/* libraries */
import styled, { keyframes } from 'styled-components';

/* animation */
export const mainTextFadeIn = keyframes`
0% {
  visibility: hidden;
  opacity: 0;
}
100% {
  visibility: visible;
  opacity: 1;
}
`;
